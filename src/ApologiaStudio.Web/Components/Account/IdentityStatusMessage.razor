@if (!string.IsNullOrWhiteSpace(Message))
{
    <p class="identity-status @(IsError ? "identity-status-error" : null)"
       role="@(IsError ? "alert" : "status")">
        @Message
    </p>
}

@code {
    [Parameter]
    public string? Message { get; set; }

    [Parameter]
    public bool IsError { get; set; }
}
